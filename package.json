{
  "name": "rooming-list-management-app",
  "version": "1.0.0",
  "description": "Full-Stack Rooming List Management App with React frontend and Node.js backend",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd backend && npm run dev",
    "client:dev": "cd frontend && npm start",
    "server:start": "cd backend && npm start",
    "client:start": "cd frontend && npm run build && serve -s build",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "test": "cd backend && npm test",
    "docker:up": "docker-compose up --build",
    "docker:down": "docker-compose down"
  },
  "keywords": [
    "rooming-list",
    "hotel-management",
    "react",
    "nodejs",
    "postgresql"
  ],
  "author": "Assistant",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "dependencies": {
    "axios": "^1.10.0"
  }
}
